export const metadata = {
  title: 'Contact Us',
  description: "Schedule your free consultation with BuyFlatFee. Bay Area buyer's agent — call, email, or book online.",
};

import ContactPage from './page.client';

export default function Page() {
  return <ContactPage />;
}
